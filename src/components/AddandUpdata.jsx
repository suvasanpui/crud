import React from "react";
import Modal from "./Modal";
import { Field, Form, Formik } from "formik";

function AddandUpdata({ open, isClose }) {
  return (
    <>
      <Modal isOpen={open} isClose={isClose}>
        
      </Modal>
    </>
  );
}

export default AddandUpdata;
